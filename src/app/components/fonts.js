import { Inter, Poppins, Nunito, Fuzzy_Bubbles, Playpen_Sans, Grandstander } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const nunito = Nunito({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const fuzzyBubbles = Fuzzy_Bubbles({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const playpenSans = Playpen_Sans({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

const grandstander = Grandstander({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});

export {
  inter,
  poppins,
  nunito,
  fuzzyBubbles,
  playpenSans,
  grandstander
};
